# gcode_syntax.vim
A vim plugin that highlights G&amp;M codes

# Installation
Load plugin into your vim plugin directory, and load the plugin
using either the :source command, or put it in your .vimrc file
to automatically load.
